현재 LOTTE HOTELS & RESORTS 프런트 스케줄 SaaS의
“스케줄 재생성 > 근무표에서 보호 일정 선택” 모달에서
기존 등록 보호 일정이 해제되지 않는 문제를 수정해줘.

이번 작업의 핵심은
새로 선택한 보호 일정뿐 아니라
이미 등록되어 있던 보호 일정도 모달 안에서 해제하거나 보호 수준을 변경할 수 있게 만드는 것이야.

--------------------------------------------------
[현재 문제]
--------------------------------------------------

현재 보호 일정 선택 모달에서 새로 선택한 셀은 다시 클릭하면 해제가 됨.

하지만 기존에 이미 등록되어 있던 보호 일정은 해제가 되지 않음.

예:
- 자동 절대 보호 4건이 기존 목록에 표시됨
- 근무표 셀에도 자물쇠 아이콘으로 표시됨
- 하지만 해당 셀을 클릭해도 보호 해제가 되지 않음
- 목록에서도 기존 보호 일정을 해제할 수 없음

이건 잘못된 동작임.

--------------------------------------------------
[수정 목표]
--------------------------------------------------

보호 일정 선택 모달에서는 아래 3가지 일정이 모두 같은 방식으로 관리되어야 함.

1) 기존 자동 보호 일정
2) 기존 수동 보호 일정
3) 이번에 새로 선택한 보호 일정

즉, 기존 보호 일정도 선택 상태로 인식되어야 하고,
사용자가 해제하거나 보호 수준을 변경할 수 있어야 함.

--------------------------------------------------
[핵심 수정 방향]
--------------------------------------------------

기존 보호 일정 데이터를 단순 표시용으로만 렌더링하지 말고,
모달 진입 시 선택 상태 데이터에 함께 로드해줘.

모달 open 시:
- existingProtectedSchedules를 불러옴
- selectedProtectedCells 초기값에 기존 보호 일정을 포함
- 각 셀은 protectionLevel, source, isExisting 값을 가지고 있어야 함

권장 데이터 구조:

selectedProtectedCells:
- id
- employeeId
- employeeName
- date
- shiftCode
- protectionLevel: absolute | priority
- source: auto | manual
- isExisting: true | false
- reason
- canRemove: true | false
- canChangeLevel: true | false

중요:
- 기존 보호 일정도 selectedProtectedCells 안에 포함되어야 함
- 새로 선택한 셀만 별도로 관리하면 안 됨
- 기존 보호 일정이 readOnly 표시만 되고 toggle 대상에서 빠지면 안 됨

--------------------------------------------------
[기존 보호 일정 해제 동작]
--------------------------------------------------

기존 보호 셀을 클릭하면 바로 무시하지 말고
보호 해제 확인창을 띄워줘.

확인창 문구:

제목:
보호 일정 해제

내용:
“이 일정은 현재 보호 일정으로 등록되어 있습니다.
보호를 해제하면 스케줄 재생성 시 변경 대상이 될 수 있습니다.
보호 설정을 해제하시겠습니까?”

버튼:
- 보호 해제
- 유지
- 취소

“보호 해제” 클릭 시:
- 해당 셀을 selectedProtectedCells에서 제거하지 말고
  removedProtectedCells에 기록하거나 status를 removed로 변경
- 근무표 셀의 보호 표시 제거
- 좌측 기존 보호 목록에서도 해제 예정 상태로 표시
- 하단 저장 버튼 카운트에서 제외

권장:
- 저장 전까지는 “해제 예정” 상태로 표시
- 최종 “보호 일정으로 저장” 클릭 시 실제 보호 목록에서 제거

--------------------------------------------------
[자동 절대 보호 해제 제한]
--------------------------------------------------

단, 일부 자동 절대 보호는 해제가 불가능하거나 강한 경고가 필요함.

자동 절대 보호 예:
- 승인된 교육
- 승인된 병가
- 직원이 신청했고 승인 안내까지 나간 휴일
- 직원에게 승인 안내가 나간 연차
- 직원에게 승인 안내가 나간 SL
- 직원에게 승인 안내가 나간 HO
- 신혼여행
- 장기 병가
- 장기 교육
- 퇴사 이후 기간

이런 항목은 기본적으로 canRemove: false 또는 strongConfirm 필요.

권장 동작:
1) 승인된 교육 / 병가 / 승인 안내 발송 휴일:
   - 일반 클릭 해제 금지
   - 해제하려면 “관리자 강제 해제” 확인창 필요

2) 스케줄러가 수동으로 추가한 보호 일정:
   - 일반 확인창 후 해제 가능

--------------------------------------------------
[관리자 강제 해제 확인창]
--------------------------------------------------

자동 절대 보호를 해제하려는 경우 아래 확인창을 띄워줘.

제목:
자동 보호 일정 해제

내용:
“이 일정은 승인 또는 안내가 완료되어 자동 절대 보호로 설정된 일정입니다.
보호를 해제하면 직원에게 이미 안내된 일정이 변경될 수 있습니다.
정말 보호를 해제하시겠습니까?”

버튼:
- 강제로 보호 해제
- 유지
- 취소

강제로 보호 해제 시:
- removedProtectedCells에 기록
- 변경 이력에 남길 수 있도록 해제 사유 입력 필드 제공 가능
- 저장 전에는 해제 예정 상태로 표시

--------------------------------------------------
[좌측 기존 보호 목록에서 해제]
--------------------------------------------------

좌측 기존 보호 일정 목록의 각 항목에도 “해제” 액션을 추가해줘.

표시:
- 자동 절대 보호 목록
- 자동 우선 보호 목록
- 수동 추가 보호 목록

각 항목 오른쪽에 작은 해제 아이콘 또는 “해제” 버튼 제공.

동작:
- 목록에서 해제 클릭
- 해당 근무표 셀 보호 표시도 함께 제거
- 저장 전까지 “해제 예정” 상태로 표시

중요:
- 목록에서 해제한 것과 근무표 셀에서 해제한 것이 같은 상태를 공유해야 함
- 목록과 셀이 따로 놀면 안 됨

--------------------------------------------------
[보호 수준 변경 동작]
--------------------------------------------------

기존 보호 일정을 다른 보호 수준으로 바꾸는 것도 가능해야 함.

예:
- 기존 우선 보호 → 절대 보호
- 기존 절대 보호 → 우선 보호

변경 시 반드시 확인창 표시.

우선 보호 → 절대 보호:
“절대 보호로 변경하면 스케줄 재생성 시 자동 변경 대상에서 제외됩니다.”

절대 보호 → 우선 보호:
“우선 보호로 변경하면 스케줄 재생성 시 관리자 확인 후 변경될 수 있습니다.”

변경 후:
- selectedProtectedCells의 protectionLevel 변경
- 셀 표시 변경
- 좌측 목록 그룹 이동
- 하단 카운트 갱신

--------------------------------------------------
[저장 동작]
--------------------------------------------------

“보호 일정으로 저장” 클릭 시 아래 내용을 실제 반영해줘.

반영 대상:
1) 새로 추가된 보호 일정
2) 해제된 기존 보호 일정
3) 보호 수준이 변경된 일정

저장 후:
- 절대 보호 건수 갱신
- 우선 보호 건수 갱신
- 해제된 일정은 보호 목록에서 제거
- 변경된 보호 수준은 새 그룹에 반영
- 스케줄 재생성 화면 보호 일정 카드에 반영

toast 문구:
제목:
보호 일정 저장 완료

설명:
“절대 보호 2건, 우선 보호 3건이 저장되었습니다. 해제 1건이 반영되었습니다.”

--------------------------------------------------
[하단 카운트 표시]
--------------------------------------------------

하단에는 현재 최종 저장될 기준으로 카운트를 보여줘.

예:
자동 보호 7건 읽기 전용 + 수동 선택 절대 2건 / 우선 0건

이 표현이 헷갈리면 아래처럼 단순화해줘.

- 절대 보호 6건
- 우선 보호 3건
- 해제 예정 1건
- 신규 추가 2건

저장 버튼:
“보호 일정으로 저장 (절대 6건 / 우선 3건 / 해제 1건)”

--------------------------------------------------
[중요 구현 포인트]
--------------------------------------------------

현재 문제의 핵심은 기존 보호 일정이 toggle 가능한 선택 상태로 들어가지 않는 것임.

따라서 아래를 반드시 수정해줘.

- 기존 보호 일정을 단순 렌더링하지 말 것
- 모달 open 시 기존 보호 일정도 selectedProtectedCells에 포함
- 기존 보호 셀 클릭 시 toggle 또는 확인창 동작 실행
- 기존 보호 목록의 해제 버튼과 근무표 셀 클릭이 같은 상태를 변경
- 저장 전 임시 상태와 저장 후 확정 상태를 구분
- 저장해야만 실제 보호 일정 데이터가 변경되도록 처리

--------------------------------------------------
[작업 후 확인]
--------------------------------------------------

수정 후 아래를 반드시 확인해줘.

1) 기존 자동 보호 일정이 모달 진입 시 표시되는가?
2) 기존 보호 일정이 근무표 셀에도 표시되는가?
3) 기존 보호 셀을 클릭하면 해제 확인창이 뜨는가?
4) 기존 보호 일정을 해제하면 셀 표시와 좌측 목록이 함께 갱신되는가?
5) 새로 선택한 보호 일정도 기존처럼 해제되는가?
6) 기존 보호와 신규 보호가 같은 selectedProtectedCells 구조에서 관리되는가?
7) 절대 보호 / 우선 보호 카운트가 정확히 갱신되는가?
8) 해제 예정 건수가 표시되는가?
9) 저장 후 해제된 일정이 보호 목록에서 사라지는가?
10) 자동 절대 보호를 해제할 때 강한 확인창이 뜨는가?

--------------------------------------------------
[기능 보호 공통 지시]
--------------------------------------------------

이번 작업은 명시된 범위만 수정하는 작업입니다.

내가 삭제하라고 직접 말하지 않은 기존 기능, 탭, 버튼, 범례, 모달, 토스트, 상태값, 데이터 연결은 절대 삭제하거나 변경하지 마세요.

새 기능을 추가하거나 UI를 수정할 때는 기존 기능 위에 추가/보완하는 방식으로 처리하고, 기존 구현을 덮어쓰지 마세요.

특히 아래 항목은 임의로 삭제하거나 숨기거나 placeholder로 되돌리면 안 됩니다.

- 좌측 메뉴 전체
- 대시보드 기존 카드
- 운영 진행 단계
- 운영 경고 요약
- 공정성 리포트
- 월별 근무표 탭
- 연간 야간조 계획 탭
- 연간 야간조 계획 실제 기능
- 근무표 상단 버튼 전체
- 근무표의 C08 / C09 / C10 / C11 중간조 코드와 범례
- EDU / SICK 범례
- 알림 발송
- 발송 이력
- 변경 이력
- 근무강도 결과
- 저장 / 확정 버튼
- 전체화면보기 연결
- 근태 신청의 교육 / 병가 신청
- 주말/공휴 근무 희망 신청 취소
- 기존 toast 발생 조건
- 기존 데이터와 상태 연결
- 직원 관리의 직원 추가 / 설정 / 전체 확정 기능
- 직원 관리의 근속 계산 기능
- 직원 관리의 직급 / 역할 / 상태 관리 기능
- 근태 관리의 상세 검토 모달
- SL을 연차로 변경하는 관리자 액션
- 교육 신청 승인/반려 흐름
- 병가 신청 승인/반려 흐름
- 기존 수요예측 업로드 및 결과 화면
- 기존 버전 관리 기능
- 기존 월별 근무표 데이터
- 기존 연간 야간조 계획 데이터
- 기존 운영 경고 패널
- 스케줄 재생성 화면 자체
- 보호 일정 선택 기능
- 야간조 조정 옵션

이번 작업과 무관한 기능 로직, 데이터 구조, 화면 전환 구조, 저장/확정 처리, 알림 발송 처리, 변경 이력 처리, 근태 신청 처리 로직은 수정하지 마세요.

수정이 필요한 경우에도 기존 기능을 제거하고 새로 만드는 방식이 아니라, 기존 기능을 보존한 상태에서 필요한 부분만 보완하세요.

작업 완료 후에는 반드시 아래 회귀 점검을 수행하세요.

1) 내가 지시하지 않았는데 사라진 메뉴가 없는가?
2) 내가 지시하지 않았는데 사라진 탭이 없는가?
3) 내가 지시하지 않았는데 사라진 버튼이 없는가?
4) 내가 지시하지 않았는데 사라진 범례가 없는가?
5) 기존 모달과 toast가 정상적으로 열리는가?
6) 저장/확정/알림/변경이력 기능이 기존처럼 동작하는가?
7) 전체화면보기가 구버전으로 롤백되지 않았는가?
8) 연간 야간조 계획이 placeholder로 바뀌지 않았는가?
9) 근무표의 C08 / C09 / C10 / C11 중간조 코드와 범례가 유지되는가?
10) EDU / SICK가 유지되는가?
11) 기존 데이터 연결이 끊기지 않았는가?
12) 직원 관리의 직원 추가 / 설정 / 전체 확정 기능이 유지되는가?
13) 근태 관리 상세 검토 모달이 유지되는가?
14) SL → 연차 변경 액션이 정상 동작하는가?
15) 교육/병가 승인 기능이 기존 근태 관리 흐름과 충돌하지 않는가?
16) 수요예측 기존 기능이 사라지지 않았는가?
17) 버전 관리 기능이 기존처럼 유지되는가?
18) 월별 근무표와 연간 야간조 계획 데이터가 서로 끊기지 않았는가?
19) 보호 일정 선택 기능이 스케줄 재생성과 연결되어 유지되는가?

최종적으로 이번 요청에서 명시한 수정사항만 반영하고, 나머지 기존 기능은 그대로 유지한 상태로 마무리하세요.