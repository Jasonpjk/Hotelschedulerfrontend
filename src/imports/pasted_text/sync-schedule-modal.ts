현재 LOTTE HOTELS & RESORTS 프런트 스케줄 SaaS의
“스케줄 재생성 > 근무표에서 보호 일정 선택” 모달 안에 표시되는 근무표가
실제 “근무표 > 월별 근무표” 화면의 스케줄표와 다르게 보이는 문제가 있습니다.

이번 작업은 보호 일정 선택 모달 안의 근무표를
실제 월별 근무표와 동일한 형식과 동일한 데이터 기준으로 맞추는 작업입니다.

--------------------------------------------------
[현재 문제]
--------------------------------------------------

현재 “근무표에서 보호 일정 선택” 모달 안에 표시되는 스케줄표가
실제 “근무표 > 월별 근무표” 화면에 있는 스케줄표와 다르게 보입니다.

문제:
- 직원 순서가 다를 수 있음
- 조 그룹 구분이 다를 수 있음
- 근무 코드 표시 방식이 다를 수 있음
- 날짜 헤더 형식이 다를 수 있음
- 셀 크기와 배지 스타일이 다를 수 있음
- 실제 월별 근무표의 최신 데이터와 다를 수 있음
- 월별 근무표에서 보이는 EDU, SICK, VAC, SL, OFF, M07, A13, N22, C08~C11 표시가 동일하게 반영되지 않을 수 있음

이러면 스케줄러가 실제 근무표와 다른 화면을 보고 보호 일정을 선택하게 되므로 위험합니다.

--------------------------------------------------
[수정 목표]
--------------------------------------------------

보호 일정 선택 모달 안의 근무표는
실제 “근무표 > 월별 근무표” 화면의 스케줄표를 그대로 가져와서 보여줘야 합니다.

즉, 별도의 간이 테이블이나 별도 mock schedule table을 만들지 말고,
기존 월별 근무표에서 쓰는 동일한 컴포넌트와 동일한 데이터를 재사용해줘.

핵심 원칙:
- 같은 직원 목록
- 같은 직원 순서
- 같은 조 그룹 구분
- 같은 날짜 헤더
- 같은 근무 코드
- 같은 배지 스타일
- 같은 셀 크기
- 같은 가로/세로 스크롤 구조
- 같은 월별 근무표 데이터
- 같은 버전 기준
- 같은 호텔 기준
- 같은 월 기준

--------------------------------------------------
[컴포넌트 재사용 원칙]
--------------------------------------------------

보호 일정 선택 모달용 근무표를 별도로 새로 만들지 말고,
기존 월별 근무표 컴포넌트를 재사용해줘.

권장 구조:
- MonthlyScheduleGrid
- ScheduleGrid
- ScheduleTable
- 또는 현재 월별 근무표 화면에서 실제로 사용하는 동일한 grid 컴포넌트

이 컴포넌트에 mode만 추가해서 사용해줘.

예:
mode = "view"
mode = "edit"
mode = "protect-selection"

보호 일정 선택 모달에서는:
mode = "protect-selection"

이 모드에서는 기존 근무표 표시 방식은 그대로 유지하고,
추가로 셀 다중 선택과 보호 수준 표시만 가능하게 해줘.

--------------------------------------------------
[데이터 기준]
--------------------------------------------------

보호 일정 선택 모달 안의 근무표는
아래 데이터를 실제 월별 근무표와 동일하게 사용해야 합니다.

- selectedHotel
- selectedMonth
- currentScheduleVersion
- scheduleGridData
- employeeList
- employeeGroupOrder
- shiftCodes
- scheduleStatus
- attendanceReflections
- annualNightPlanReflections
- protectedScheduleCells

중요:
- 보호 일정 선택 모달 전용 mock 데이터 사용 금지
- 보호 일정 선택 모달 전용 임시 직원 목록 사용 금지
- 보호 일정 선택 모달 전용 임시 스케줄 데이터 사용 금지
- 실제 월별 근무표와 다른 데이터 source 사용 금지

--------------------------------------------------
[표시 형식 일치]
--------------------------------------------------

보호 일정 선택 모달의 근무표는
실제 월별 근무표와 아래 항목이 모두 같아야 합니다.

1) 직원 영역
- 직원명
- 직급
- 역할
- 조 그룹
- 인차지 표시
- 직원 순서
- 조 그룹 구분선

2) 날짜 헤더
- 날짜
- 요일
- 주말 색상
- 공휴일 표시
- 기준일 이전 잠금 표시

3) 근무 코드 배지
- M07
- A13
- N22
- C08
- C09
- C10
- C11
- OFF
- HOL
- VAC
- SL
- EDU
- SICK
- REQ

4) 셀 스타일
- 실제 월별 근무표와 같은 셀 높이
- 같은 셀 너비
- 같은 배지 크기
- 같은 폰트
- 같은 색상
- 같은 테두리

5) 스크롤
- 직원명 영역 고정
- 날짜 영역 가로 스크롤
- 직원 행 세로 스크롤
- 월말일까지 표시
- 하단 버튼에 스크롤바가 가려지지 않게 처리

--------------------------------------------------
[보호 일정 선택 모드에서 추가되는 기능]
--------------------------------------------------

기존 월별 근무표 컴포넌트를 그대로 사용하되,
보호 일정 선택 모드에서는 아래 기능만 추가해줘.

1) 셀 다중 선택
- 변경 기준일 이후 셀만 선택 가능
- 기준일 이전 셀은 비활성 또는 잠금 표시

2) 보호 수준 표시
- 절대 보호
- 우선 보호

3) 기존 보호 일정 표시
- 이미 등록된 보호 일정은 셀 위에 아이콘 또는 테두리로 표시
- 절대 보호: 자물쇠 아이콘
- 우선 보호: 방패 아이콘

4) 기존 보호 일정 해제
- 기존 보호 셀 클릭 시 해제 확인창 표시
- 해제 후 저장 시 반영

5) 보호 수준 변경
- 절대 보호 ↔ 우선 보호 변경 시 확인창 표시

중요:
- 선택 기능을 추가하더라도 기존 근무표 레이아웃을 깨면 안 됨
- 근무 코드 배지가 보호 아이콘에 가려지면 안 됨
- 셀 선택 때문에 기존 코드가 안 보이면 안 됨

--------------------------------------------------
[월별 근무표와 차이가 나면 안 되는 항목]
--------------------------------------------------

아래 항목은 실제 월별 근무표와 반드시 일치해야 합니다.

- 호텔
- 월
- 버전
- 직원 목록
- 직원 정렬
- 조 그룹
- 날짜 수
- 근무 코드
- 휴무 코드
- 교육/병가 코드
- 연차/SL/HO 표시
- 야간조 표시
- 중간조 표시
- 공휴일 표시
- 근무표 데이터

예:
실제 월별 근무표에서 5월 6일 박지현이 M07이면
보호 일정 선택 모달에서도 5월 6일 박지현은 반드시 M07로 보여야 함.

실제 월별 근무표에서 5월 10일 이수진이 SL이면
보호 일정 선택 모달에서도 반드시 SL로 보여야 함.

--------------------------------------------------
[모달 크기와 스크롤]
--------------------------------------------------

보호 일정 선택 모달은 실제 월별 근무표를 충분히 볼 수 있게 조정해줘.

수정:
- 모달 너비를 현재보다 넓게 유지
- 높이는 화면 안에서 최대한 확보
- 내부 스크롤 영역 분리
- 좌측 기존 보호 목록과 우측 근무표가 서로 겹치지 않게 배치
- 근무표 날짜 영역은 월말일까지 가로 스크롤 가능
- 하단 저장/취소 버튼은 sticky로 두되 가로 스크롤바를 가리지 않게 처리

--------------------------------------------------
[기존 보호 목록과 근무표 연동]
--------------------------------------------------

좌측 기존 보호 목록에서 항목을 클릭하면
우측 근무표의 해당 셀 또는 기간이 보이도록 스크롤 이동해줘.

예:
좌측 목록에서 “최유진 05-01 ~ 05-14 신혼여행” 클릭
→ 우측 근무표에서 최유진 행과 05-01 부근으로 이동
→ 해당 보호 구간이 표시됨

반대로 우측 근무표에서 보호된 셀을 클릭하면
좌측 목록의 해당 보호 항목도 선택 상태로 표시해줘.

--------------------------------------------------
[하지 말 것]
--------------------------------------------------

이번 작업에서 아래는 하지 마.

- 보호 일정 선택용 별도 스케줄표 새로 만들기 금지
- 월별 근무표와 다른 직원 목록 사용 금지
- 월별 근무표와 다른 schedule mock 데이터 사용 금지
- 기존 월별 근무표 컴포넌트를 복사해서 별도 관리 금지
- 실제 근무표와 다른 배지 스타일 사용 금지
- 기존 보호 일정 선택 기능 삭제 금지
- 기존 해제/수준 변경 기능 삭제 금지

--------------------------------------------------
[작업 후 확인]
--------------------------------------------------

수정 후 아래를 반드시 확인해줘.

1) 보호 일정 선택 모달의 근무표가 실제 월별 근무표와 같은 데이터인가?
2) 직원 목록과 순서가 실제 월별 근무표와 같은가?
3) 조 그룹 구분이 실제 월별 근무표와 같은가?
4) 날짜 헤더와 요일 표시가 실제 월별 근무표와 같은가?
5) 근무 코드 배지 스타일이 실제 월별 근무표와 같은가?
6) EDU / SICK / VAC / SL / HOL / OFF가 실제 근무표와 동일하게 보이는가?
7) M07 / A13 / N22 / C08~C11이 실제 근무표와 동일하게 보이는가?
8) 월말일까지 가로 스크롤로 볼 수 있는가?
9) 보호 일정 선택 기능이 기존처럼 작동하는가?
10) 기존 보호 일정 해제 기능이 유지되는가?
11) 보호 수준 변경 확인창이 유지되는가?
12) 보호 일정 저장 후 스케줄 재생성 화면에 반영되는가?

--------------------------------------------------
[기능 보호 공통 지시]
--------------------------------------------------

이번 작업은 명시된 범위만 수정하는 작업입니다.

내가 삭제하라고 직접 말하지 않은 기존 기능, 탭, 버튼, 범례, 모달, 토스트, 상태값, 데이터 연결은 절대 삭제하거나 변경하지 마세요.

새 기능을 추가하거나 UI를 수정할 때는 기존 기능 위에 추가/보완하는 방식으로 처리하고, 기존 구현을 덮어쓰지 마세요.

특히 아래 항목은 임의로 삭제하거나 숨기거나 placeholder로 되돌리면 안 됩니다.

- 좌측 메뉴 전체
- 대시보드 기존 카드
- 운영 진행 단계
- 운영 경고 요약
- 공정성 리포트
- 월별 근무표 탭
- 연간 야간조 계획 탭
- 연간 야간조 계획 실제 기능
- 근무표 상단 버튼 전체
- 월별 근무표의 실제 스케줄표
- 근무표의 C08 / C09 / C10 / C11 중간조 코드와 범례
- EDU / SICK 범례
- 알림 발송
- 발송 이력
- 변경 이력
- 근무강도 결과
- 저장 / 확정 버튼
- 전체화면보기 연결
- 근태 신청의 교육 / 병가 신청
- 주말/공휴 근무 희망 신청 취소
- 기존 toast 발생 조건
- 기존 데이터와 상태 연결
- 직원 관리의 직원 추가 / 설정 / 전체 확정 기능
- 직원 관리의 근속 계산 기능
- 직원 관리의 직급 / 역할 / 상태 관리 기능
- 근태 관리의 상세 검토 모달
- SL을 연차로 변경하는 관리자 액션
- 교육 신청 승인/반려 흐름
- 병가 신청 승인/반려 흐름
- 기존 수요예측 업로드 및 결과 화면
- 기존 버전 관리 기능
- 기존 월별 근무표 데이터
- 기존 연간 야간조 계획 데이터
- 기존 운영 경고 패널
- 스케줄 재생성 화면 자체
- 보호 일정 선택 기능
- 기존 보호 일정 해제 기능
- 보호 수준 변경 기능
- 야간조 조정 옵션

이번 작업과 무관한 기능 로직, 데이터 구조, 화면 전환 구조, 저장/확정 처리, 알림 발송 처리, 변경 이력 처리, 근태 신청 처리 로직은 수정하지 마세요.

수정이 필요한 경우에도 기존 기능을 제거하고 새로 만드는 방식이 아니라, 기존 기능을 보존한 상태에서 필요한 부분만 보완하세요.

작업 완료 후에는 반드시 아래 회귀 점검을 수행하세요.

1) 내가 지시하지 않았는데 사라진 메뉴가 없는가?
2) 내가 지시하지 않았는데 사라진 탭이 없는가?
3) 내가 지시하지 않았는데 사라진 버튼이 없는가?
4) 내가 지시하지 않았는데 사라진 범례가 없는가?
5) 기존 모달과 toast가 정상적으로 열리는가?
6) 저장/확정/알림/변경이력 기능이 기존처럼 동작하는가?
7) 전체화면보기가 구버전으로 롤백되지 않았는가?
8) 연간 야간조 계획이 placeholder로 바뀌지 않았는가?
9) 월별 근무표 실제 스케줄표가 유지되는가?
10) 근무표의 C08 / C09 / C10 / C11 중간조 코드와 범례가 유지되는가?
11) EDU / SICK가 유지되는가?
12) 기존 데이터 연결이 끊기지 않았는가?
13) 직원 관리의 직원 추가 / 설정 / 전체 확정 기능이 유지되는가?
14) 근태 관리 상세 검토 모달이 유지되는가?
15) SL → 연차 변경 액션이 정상 동작하는가?
16) 교육/병가 승인 기능이 기존 근태 관리 흐름과 충돌하지 않는가?
17) 수요예측 기존 기능이 사라지지 않았는가?
18) 버전 관리 기능이 기존처럼 유지되는가?
19) 월별 근무표와 연간 야간조 계획 데이터가 서로 끊기지 않았는가?
20) 보호 일정 선택 기능이 스케줄 재생성과 연결되어 유지되는가?

최종적으로 이번 요청에서 명시한 수정사항만 반영하고, 나머지 기존 기능은 그대로 유지한 상태로 마무리하세요.