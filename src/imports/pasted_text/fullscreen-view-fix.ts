현재 LOTTE HOTELS & RESORTS 프런트 스케줄 SaaS에서
“연간 야간조 계획” 기능은 다시 살아났지만,
여전히 오른쪽 위 “전체화면 보기 열기”에 최신 화면이 반영되지 않고 있어.

이번 작업은 절대 전체 기능을 다시 만들거나 복원하는 작업이 아니야.
이미 살아난 메인 근무표 화면과 연간 야간조 계획은 건드리지 말고,
오직 “전체화면 보기 열기”가 최신 메인 화면과 동일한 컴포넌트/상태를 보도록 연결만 수정해줘.

--------------------------------------------------
[현재 문제]
--------------------------------------------------

메인 근무표 화면에서는 최신 기능이 정상적으로 보임:
- 월별 근무표 탭
- 연간 야간조 계획 탭
- EDU 교육 범례
- SICK 병가 범례
- 알림 발송
- 발송 이력
- 변경 이력
- 전월 근무강도 회복 결과
- 새 버전 생성
- 다운로드
- 저장
- 확정

하지만 오른쪽 위 “전체화면 보기 열기”를 누르면
전체화면에서는 최신 메인 화면이 아니라
예전 화면 또는 별도 화면이 열리고 있음.

즉, 전체화면보기만 아직 old component / preview component / 별도 hardcoded UI를 보고 있는 상태로 보임.

--------------------------------------------------
[이번 수정의 핵심 목표]
--------------------------------------------------

전체화면보기는 메인 근무표 화면과 완전히 같은 화면이어야 함.

정확히는:
- 같은 호텔
- 같은 월
- 같은 버전
- 같은 활성 탭
- 같은 버튼
- 같은 범례
- 같은 데이터
- 같은 경고 수
- 같은 저장 상태
- 같은 모달 연결
- 같은 토스트 시스템

을 사용해야 함.

전체화면보기는 별도 기능 화면이 아니라
현재 메인 ScheduleView를 더 넓은 레이아웃으로 보여주는 것뿐이어야 함.

--------------------------------------------------
[절대 건드리지 말 것]
--------------------------------------------------

아래 기능은 이미 살아났으므로 삭제하거나 수정하지 말 것.

- 연간 야간조 계획 탭
- 연간 야간조 계획 실제 화면
- 1월~12월 월별 계획 카드
- 연간 야간조 계획 저장/확정/변경 이력
- 월별 근무표 화면
- EDU 교육 범례
- SICK 병가 범례
- 알림 발송
- 발송 이력
- 전월 근무강도 회복 결과
- 근태 신청의 교육/병가 신청
- 주말/공휴 근무 희망 취소 기능

이번 작업은 “전체화면 보기 연결 문제”만 수정하는 작업이야.
다른 기능을 다시 복원하거나 새로 만들려고 하지 말 것.

--------------------------------------------------
[원인 점검]
--------------------------------------------------

먼저 전체화면 보기 버튼이 실제로 어떤 컴포넌트를 여는지 확인해줘.

아래와 같은 구조가 있으면 문제 원인일 가능성이 높음:
- FullscreenSchedule
- ScheduleFullscreen
- SchedulePreview
- PreviewSchedulePage
- OldScheduleView
- 별도 fullscreen 전용 toolbar
- 별도 fullscreen 전용 legend
- 별도 fullscreen 전용 mock data
- 별도 fullscreen 전용 activeTab state
- 전체화면 보기 클릭 시 route가 바뀌면서 기존 state가 초기화되는 구조

이런 구조가 있으면 제거하거나 최신 메인 ScheduleView를 재사용하도록 바꿔줘.

--------------------------------------------------
[수정 방향]
--------------------------------------------------

전체화면보기는 반드시 메인 근무표에서 사용하는 최신 컴포넌트를 그대로 사용해야 함.

권장 구조:

- SchedulePageContainer
  - selectedHotel
  - selectedMonth
  - selectedVersion
  - activeTab
  - scheduleStatus
  - toolbarActions
  - legendItems
  - scheduleData
  - warningCount
  - unsavedChanges
  - notificationModalState
  - historyModalState
  - annualNightPlanState

- ScheduleView
  - ScheduleTabs
  - ScheduleToolbar
  - ScheduleLegend
  - ScheduleGrid
  - AnnualNightShiftPlan
  - ScheduleBottomAiPanel

일반 화면:
- <ScheduleView mode="normal" />

전체화면:
- <ScheduleView mode="fullscreen" />

중요:
- mode가 fullscreen이라고 해서 기능이 빠지면 안 됨
- mode는 레이아웃, 폭, 높이, 스크롤 처리만 바꿔야 함
- 버튼 배열, 범례 배열, 데이터 배열을 fullscreen용으로 다시 만들면 안 됨

--------------------------------------------------
[상태 공유 규칙]
--------------------------------------------------

전체화면보기와 메인 화면은 반드시 같은 state/store/context를 사용해야 함.

공유해야 하는 상태:
- selectedHotel
- selectedMonth
- selectedVersion
- activeTab
- scheduleStatus
- warningCount
- toolbarActions
- legendItems
- scheduleGridData
- annualNightPlanData
- unsavedChanges
- openedModals
- toast system

특히 activeTab도 공유해야 함.
예:
- 메인에서 월별 근무표 탭이면 전체화면도 월별 근무표
- 메인에서 연간 야간조 계획 탭이면 전체화면도 연간 야간조 계획

--------------------------------------------------
[전체화면 보기 버튼 동작]
--------------------------------------------------

오른쪽 위 “전체화면 보기 열기” 버튼 클릭 시:
- 현재 메인 화면 상태를 유지한 채 fullscreen shell만 열기
- route 이동으로 상태를 초기화하지 말 것
- old preview page로 이동하지 말 것
- fullscreen 전용 mock 화면을 열지 말 것

닫기 시:
- 메인 화면으로 돌아와도 기존 상태 유지
- 메인 화면이 예전 버전으로 바뀌면 안 됨

--------------------------------------------------
[전체화면에서 반드시 보여야 하는 항목]
--------------------------------------------------

전체화면보기에서도 아래 항목이 메인 화면과 동일하게 보여야 함.

탭:
- 월별 근무표
- 연간 야간조 계획

상단 버튼:
- 월 선택
- 버전 선택
- 작업 중 / 확정 상태
- 축소
- 확대
- 초기화
- 경고
- 변경 이력
- 알림 발송
- 발송 이력
- 전월 근무강도 회복 결과
- 새 버전 생성
- 다운로드
- 저장
- 확정

근무 범례:
- M07
- A13
- N22
- C08
- C09
- C10
- C11

휴무 범례:
- REQ
- OFF
- HOL
- VAC
- SL
- EDU 교육
- SICK 병가

조 그룹:
- 오전조
- 오후조
- 야간조

연간 야간조 계획 탭에서는:
- 준비 중 placeholder 금지
- 실제 연간 야간조 계획 화면 표시
- 1월~12월 계획 카드 표시
- 저장/확정/변경 이력 유지

--------------------------------------------------
[중요한 수정 방식]
--------------------------------------------------

이번에는 절대 메인 화면을 전체화면에 맞추지 말고,
전체화면을 메인 화면에 맞춰줘.

잘못된 수정:
- 전체화면의 예전 UI를 메인 화면에 덮어쓰기
- 메인 화면을 old fullscreen component 기준으로 롤백
- 최신 기능이 빠진 fullscreen 전용 toolbar를 계속 사용

올바른 수정:
- 메인 화면의 최신 ScheduleView를 기준으로 삼음
- 전체화면은 그 ScheduleView를 fullscreen layout으로 감쌈
- 기능/상태/데이터는 모두 동일하게 공유

--------------------------------------------------
[작업 후 확인]
--------------------------------------------------

수정 후 아래를 반드시 확인해줘.

1) 메인 화면은 기존 최신 기능을 그대로 유지하는가?
2) 전체화면 보기 열기 버튼을 눌렀을 때 메인 화면과 동일한 화면이 나오는가?
3) 전체화면에서 EDU / SICK 범례가 보이는가?
4) 전체화면에서 알림 발송 / 발송 이력 / 전월 근무강도 회복 결과가 보이는가?
5) 전체화면에서 연간 야간조 계획 탭이 보이는가?
6) 전체화면에서 연간 야간조 계획 탭을 누르면 실제 기능 화면이 나오는가?
7) 전체화면을 닫아도 메인 화면이 예전 버전으로 롤백되지 않는가?
8) 이번 작업과 무관한 기존 기능이 사라지지 않았는가?

--------------------------------------------------
[최종 지시]
--------------------------------------------------

이번 수정은 전체화면보기 연결만 고치는 작업이야.

연간 야간조 계획은 이미 살아났으니 다시 placeholder로 바꾸지 말고,
메인 화면도 다시 예전 버전으로 롤백하지 마.

전체화면보기에서 old component를 쓰고 있으면 제거하고,
현재 메인 화면에서 사용하는 최신 ScheduleView / SchedulePageContainer를 그대로 재사용해줘.

전체화면은 “다른 화면”이 아니라
“같은 화면의 넓은 보기”여야 해.